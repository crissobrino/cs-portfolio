Drop images for this project here:

- `cover.jpg`             — original digit next to reconstructions at 90%,
  50% and 10% visible. First image in the gallery.
- `entropy.jpg`           — pixel-wise entropy over 50 completions.
  Second image in the gallery.
- `transformer-vs-cnn.jpg` — Transformer vs CNN imputation quality across
  visibility levels. Third image in the gallery.
- `thumb.jpg`             — reserved for a future project-card thumbnail
  (not wired up in the site yet, same as the other projects' reserved
  thumb).

If you drop in a different file extension (.png, etc.), tell me and I'll
update the `src` paths in `assets/js/projects.js` to match — filenames
just need to match what's referenced there.

Delete this README once real images are in place.
