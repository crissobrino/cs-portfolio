Images for this project, already in place:

- `cover.jpg`  — original fundus photograph next to the same image after
  Ben Graham contrast normalisation. First image in the gallery.
- `result.jpg` — bar chart of private-set AUC for every submission across
  both tracks (the lowest bar is the dtype bug that turned every image
  grey). Second image in the gallery.
- `thumb.jpg`  — reserved for a future project-card thumbnail (not wired
  up in the site yet, same as the other projects' reserved thumb).

Paths and captions are set in `assets/data/projects/diabetic-retinopathy.json`.
To swap an image, replace the file here (same filename) or update the
`src` there.
