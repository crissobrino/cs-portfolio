Drop images for this project here:

- `cover.jpg`  — Product-of-Experts architecture: three encoders fusing
  into one shared latent distribution, then three decoders. First image
  in the gallery.
- `result.jpg` — insulin reconstruction trace, showing the model flattens
  sharp bolus spikes into a smooth average. Second image in the gallery.
- `thumb.jpg`  — reserved for a future project-card thumbnail (not wired
  up in the site yet, same as the other projects' reserved thumb).

If you drop in a different file extension (.png, etc.), tell me and I'll
update the `src` paths in `assets/js/projects.js` to match — filenames
just need to match what's referenced there.

Delete this README once real images are in place.
